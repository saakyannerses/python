import math

a = float(input("Введите значение a: "))
b = float(input("Введите значение b: "))

x = (2 / (a * a + 25) / math.sqrt(b) + ((a + b) / 2))

y = abs(a) + 2 * math.sin(b) / 5.5 * a

print("Значение x: ", x)
print("Значение y: ", y)

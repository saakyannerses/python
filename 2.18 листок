import math

x = float(input("Введите значение x: "))
y = float(input("Введите значение y: "))

z = (x + (2 + y) / (x * x)) / (y + 1 / math.sqrt(x * x + 10))
q = 7.2 * math.sin(x) - abs(y)

print("Значение z: ", z)
print("Значение q: ", q)
